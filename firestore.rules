rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ===============================================================
    // Assumed Data Model
    // ===============================================================
    //
    // Collection: exams
    // Document ID: {examId}
    // Fields:
    //   - id: string (required) - Unique ID
    //   - name: string (required) - Exam name
    //   - targetDate: string (required) - ISO 8601 date-time
    //   - description: string (optional) - Description
    //
    // Collection: settings
    // Document ID: config
    // Fields:
    //   - backgroundUrl: string (optional) - Background image URL
    //   - theme: string (optional) - Theme name (light, dark, glass)
    //
    // ===============================================================

    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }

    function isAdmin() {
      return isAuthenticated() && (
        request.auth.token.email == "ywsfkrt300@gmail.com" && request.auth.token.email_verified == true
      );
    }

    // Exam rules
    match /exams/{examId} {
      allow read: if true; // Publicly readable
      allow write: if true; // Temporary allow for bot writes
    }

    // Settings rules
    match /settings/config {
      allow read: if true; // Publicly readable
      allow write: if true; // Temporary allow for bot writes
    }

    // Notification rules
    match /notifications/{notificationId} {
      allow read: if true; // Publicly readable
      allow write: if true; // Bot can send notifications
    }

    // Presence rules
    match /presence/{sessionId} {
      allow read: if true; // Bot can count users
      allow write: if true; // Clients can update presence
    }

    // Subject rules
    match /subjects/{subjectId} {
      allow read: if true; // Publicly readable
      allow write: if true; // Bot can add subjects
    }

    // Progress rules
    match /progress/{progressId} {
      allow read: if true; // Publicly readable
      allow write: if true; // Clients can update progress
    }
  }
}
